interface:
  display_name: "Arium Builder"
  short_description: "Repo-specific patterns and workflows for arium-builder"
  default_prompt: "Use the arium-builder repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true